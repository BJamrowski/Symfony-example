{% extends('base.html.twig') %}

{% block body %}
    {{ car.model }}
    <img src = "{{ '/uploads/'~car.image }}" class="img-fluid" alt="Responsive image" width="803px" height="255px">
{% endblock %}